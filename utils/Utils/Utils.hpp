#ifndef UTILS_H
#define UTILS_H

#include <iostream>
#include <vector>
#include <string>

using namespace std;

class Utils {
    public:
        static vector<string> split(const string &, char);
};

#endif